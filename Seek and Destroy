function destroyer(arr) {
  
  var initialArray=[];
  for (i=0;i<arr[0].length;i++){
    initialArray.push(arr[0][i]);
  }
 
  var newArray=[];
  var numberArguments=arr.length-1; 
  var argument;
  
  
  for (i=0;i<numberArguments;i++){
    argument=arr[i+1];
 newArray=initialArray.filter(function(num){
   return (num!==argument);
                              });
  
  }
return newArray;
}

destroyer([[1, 2, 3, 1, 2, 3], 2, 3]);
